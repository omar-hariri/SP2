  # configs/rf.yaml
wandb:
  project: SP2
  experiment_name: RF_LODO_CV
  entity: omarhariri-syrian-private-university

model:
  n_estimators: 700
  max_depth: 50
  min_samples_split: 20
  min_samples_leaf: 10
  max_features: "sqrt"
  class_weight: "balanced"
  random_state: 42
  n_jobs: -1

output:
  class_names: ["NON_AGGRESSIVE", "AGGRESSIVE"]
